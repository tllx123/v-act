import { underscore } from '@v-act/vjs.framework.extension.vendor.underscore'

const clone = underscore.clone
const defaults = underscore.defaults
const extend = underscore.extend
const functions = underscore.functions
const has = underscore.has
const invert = underscore.invert
const isArguments = underscore.isArguments
const isArray = underscore.isArray
const isBoolean = underscore.isBoolean
const isDate = underscore.isDate
const isElement = underscore.isElement
const isEmpty = underscore.isEmpty
const isEqual = underscore.isEqual
const isFunction = underscore.isFunction
const isNull = underscore.isNull
const isNumber = underscore.isNumber
const isObject = underscore.isObject
const isRegExp = underscore.isRegExp
const isString = underscore.isString
const isUndefined = underscore.isUndefined
const keys = underscore.keys
const omit = underscore.omit
const pairs = underscore.pairs
const pick = underscore.pick
const values = underscore.values

export {
  clone,
  defaults,
  extend,
  functions,
  has,
  invert,
  isArguments,
  isArray,
  isBoolean,
  isDate,
  isElement,
  isEmpty,
  isEqual,
  isFunction,
  isNull,
  isNumber,
  isObject,
  isRegExp,
  isString,
  isUndefined,
  keys,
  omit,
  pairs,
  pick,
  values
}
